cluster:
  - "cardinal"
form:
  - auto_modules_schrodinger
  - auto_accounts
  - bc_num_hours
  - bc_num_slots
  - num_cores
  - node_type
  - licenses
  - bc_vnc_resolution
  - bc_email_on_started
attributes:
  auto_modules_schrodinger:
    default: false
    help: "This defines the version of Schrodinger you want to load."
    label: "Schrodinger version"
  num_cores:
    widget: "number_field"
    label: "Number of cores"
    value: 1
    help: |
      Number of cores on node type (4.84 GB usable memory per core).
    min: 1
    max: 96
    step: 1
  bc_vnc_resolution:
    required: true
  node_type:
    widget: select
    label: "Node type"
    help: |
      - **any** - (*96 cores*) Use any available Cardinal node(s). This reduces
        the wait time as there are no node requirements.
      - **vis** - (*96 cores*) Use Cardinal node(s) that have an [NVIDIA H100
        GPU](https://www.nvidia.com/en-us/data-center/h100/) with an X server
        running in the background. This utilizes the GPU for hardware
        accelerated 3D visualization. There are 32 of these nodes.
      - **hugemem** - (*96 cores*) Use Cardinal node(s) that have 2 TB of
        available RAM. There are 16 of these nodes.
        Requesting these nodes always reserves entire nodes.
    options:
      - [
          'any', 'any',
          data-max-num-cores: 96,
          data-min-num-cores: 1,
        ]
      - [
          'vis', 'vis',
          data-max-num-cores: 96,
          data-min-num-cores: 1,
        ]
      - [
          'hugemem', 'hugemem',
          data-max-num-cores: 96,
          data-min-num-cores: 43,
        ]
  licenses:
    help: |
      If you want to use the macromodel, glide, ligprep, qikprep, or epik
      features in Schrodinger, you need to request a license.  Licenses are
      specified in a comma separated list in the format '<name>@osc:<# of licenses>'
      such as 'glide@osc:1'.

